export type SubHeadingProps = {
    title: string;
}

export type MenuItemProps = {
    title: string;
    price: string;
    tags: string;
}

export type AwardCardProps = {
    award: {
        imgUrl: string;
        title: string;
        subtitle: string;
    };
}